# Production Preparation

This directory contains release-preparation helpers for constrained or shared-hosting deployments.

## Files

- `prepare_release.php` - Validates a local build against a target profile and writes release metadata.
- `profiles/` - Example and project-specific deployment profiles.
- `output/` - Generated reports and lock snapshots (gitignored).
